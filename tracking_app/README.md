# Tracking app

## Setup
You can create and activate a virtual environment to run the app as follows:
```
python3 -m venv venv
source venv/bin/activate
```

## To run
To start the app:
`python main.py`
